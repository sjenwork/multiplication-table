#!/usr/bin/env bash
set -euo pipefail

version_stamp="$(date +%Y%m%d-%H%M%S)"
PWA_VERSION="$version_stamp" npm run build
echo "pre-commit: verified Svelte/Vite PWA build version ${version_stamp}" >&2
